# Site principal (build produção): deixe vazio — o hub usa /api no mesmo domínio.
# Só preencha para npm run dev apontar ao servidor remoto:
# VITE_API_BASE_URL=https://56.126.163.148/caminho/api
#
# Dados fictícios no dev (opcional):
# VITE_USE_MOCK=1
# Se você for usar proxy do Vite para a API (vite.config.ts):
# VITE_API_PROXY_TARGET=http://localhost

VITE_API_BASE_URL=
